// Array 

const Array = [ 1,2,3,4,5,6,7 ]
// console.log(Array.length);

// Array.push(9); this is used for the add element in the end of array
// Array.pop(); this is used for the remove element  in the end of the array
// console.log(Array.slice(1,3)); This method is used to extract a portion of an array. For example, the following code will extract the elements from index 1 to 3 (exclusive):
// console.log(Array.splice(1,3));  this method is used for the remove or add element from the orignal array.
console.log(Array.unshift(9));
console.log(Array.shift());
console.log(Array);



